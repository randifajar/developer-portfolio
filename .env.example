# Public site URL used for canonical links, Open Graph metadata, and the sitemap.
# Optional during local development; a localhost fallback is applied.
# Release validation requires an absolute HTTPS URL in production.
# This is public metadata, not a secret.
SITE_URL=http://localhost:3000
